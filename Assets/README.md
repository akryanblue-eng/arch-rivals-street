# Assets (placeholder)

This folder is intentionally empty. It exists so the project satisfies the
Unity recoverability probe's structural check (`Assets/`, `Packages/manifest.json`,
`ProjectSettings/ProjectVersion.txt`).

The real Arch Rivals: Street scenes, scripts, and art have not been pushed
here yet. Replace this file once the actual project content is added.
